{
    "mode": "delete",
    "filter": {
        "type": "object",
        "properties": {
            "newID": {
                "type": "string"
            }
        },
        "required": [
            "newID"
        ]
    }
}